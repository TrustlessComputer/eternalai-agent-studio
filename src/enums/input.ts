export enum InputType {
  TEXT = 'text',
  DROPDOWN = 'dropdown',
  TEXTAREA = 'textarea',
  DATETIME = 'datetime',
  FILE = 'file',
  BOOLEAN = 'boolean',
  OBJECT = 'object',
  CUSTOM = 'custom',
}
